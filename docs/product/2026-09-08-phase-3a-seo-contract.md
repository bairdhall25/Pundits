# Phase 3 implemented SEO contract

Status: Evidence. Date: 2026-09-08.

This is the implemented page-type contract for receipts, game comparisons, pundit profiles, team pages, league slates, and weekly archives. It does not create duplicate routes for wording variants. Phase 3A shipped receipts/games/profiles; Phase 3B adds team, league, and week templates on the same helpers.

Shared rules: existing URLs; existing card previews; no SportsEvent markup; no FAQPage on these templates; schema and snippets derive from the same public content helpers in `lib/page-content.ts`; crawler access and `max-image-preview:large` unchanged. Empty team shells stay noindex until a mapped pick lands. Week archives stay indexable under the existing archive rules.

| Type | Intent / search question | Content floor | Title / H1 | Schema | Internal links | Lifecycle | Analytics |
|---|---|---|---|---|---|---|---|
| Receipt `/picks/{slug}/{punditId}` | Who did this pundit pick, why, and was it right? | Direct answer first; source identity/date/locator; actual rationale only if present (winner-only stays short); publisher and timestamps; named disagreement; dated optional Kalshi snapshot; result and winner-only grading scope | Title and H1 from `takeHeadline` (`{name} picks/picked {team} over {team}` plus verdict when graded). No “best experts” claims | `NewsArticle` from the same `pickStory` paragraphs as the visible article. Pundit is `mentions`, not author | Game comparison (or Market page on futures), source, profile, teams, week archive. Previous/next exist but are not the only path | One permanent URL. Grade updates `dateModified` / visible result; unknown `firstPublishedAt` stays omitted | `pick_story_open` with `page_type=receipt`, `event_slug`, `pundit_id`, `status`, `surface` |
| Game `/picks/{slug}` | Who picked each side of this matchup? | Named sides, tracked count, named disagreement when both sides exist, honest empty sides, event date, result, accessible source-backed receipt list. Never a complete survey of all experts | Title: pending `{matchup}: who picked whom` (date when known); graded `{winner} beat {loser}: who called it`. H1 is the matchup title | `WebPage` with the comparison description. `about` SportsTeam names only. No SportsEvent / Event | Individual receipts; relevant team pages; week archive when the event has season/week | Same season-qualified URL before and after the game. Empty shells stay noindex until a mapped pick lands | `event_detail_open` with `page_type=game`, `event_slug`, `sport`, `surface` |
| Profile `/pundits/{id}` | What are this person's current picks and tracked record? | Outlet identity; current mapped picks; dated 2026 record with graded sample; linked historical receipts; explicit tracked-sample disclaimer | Title: `{name}: current picks and tracked record`. H1 is the person name | `WebPage` with `mainEntity` Person, plus Person. No career-skill claim | Current and past CallCards link to receipts and sources | Durable person URL. Empty shells stay noindex until the first tracked call | `pundit_profile_open` with `page_type=profile`, `pundit_id`, `surface=profile` |
| Team `/teams/{id}` | Who is picking this team this week? | Next covered matchup and named picks; honest empty side vs no captured pick vs no scheduled game; tracked-subset disclaimer; relevant historical results and receipts | Title: pending `{team}: who is picking them vs {opponent}`; history-only `{team}: tracked picks and results`; otherwise `{team}: tracked picks`. H1 is the team name. No “best experts” claims | `WebPage` with `mainEntity` SportsTeam, plus SportsTeam. No SportsEvent | Game comparison, league slate, week archive, receipt list | Existing team URL. Empty shells stay noindex until a mapped pick lands | `team_page_open` with `page_type=team`, `team_id`, `sport`, `surface=team` |
| League `/nfl/`, `/ncaaf/` | What tracked picks are on this week's NFL/college slate? | Current live-week board (unchanged sorting/cards); game dates/status; link from each live week to its permanent archive | Title: open `{league} Week {n}: who picked whom`; settled live week `{league} Week {n}: who called it`. H1 is NFL / College football | `CollectionPage` from the same lede/description | Week archives, game cards, team index | Current league URL; live-week display rules unchanged | `league_page_open` with `page_type=league`, `sport`, `surface=ncaaf\|nfl` |
| Weekly archive `/{sport}/{season}/week-{n}/` | What were the week's picks and who got them right? | Week/year/league; covered games; verified disagreement synthesis with linked receipts; graded sample; compact graded-pick index. No per-grade news URL | Title: pending `{league} Week {n}: who picked whom ({season})`; graded `{league} Week {n}: who got them right ({season})`. H1 is `{league} Week {n}` | `CollectionPage` from the same archive description | Receipts, game comparisons, current slate, adjacent weeks | Same season/week URL from open picks to final results. Archives stay indexable | `week_archive_open` with `page_type=week`, `sport`, `season`, `week`, `surface=week` |

How each template answers its question:

- Receipt: the H1 is the pick and result; the receipt block is the evidence; story paragraphs do not synthesize a missing rationale.
- Game: the lede names who picked each team, including empty sides, without opening a card; the receipt list is crawlable HTML.
- Profile: current vs past are separate sections; the 2026 line states the graded sample and that it is not a career record or skill score.
- Team: the lede names the next covered matchup or states that no game is on the board; no captured pick is not phrased as a missing game; past results follow with receipt links.
- League: the live-week board remains the discovery surface; each week block and the lede point at the permanent week archive.
- Weekly archive: open weeks keep pending copy on the same URL; graded weeks recap verified disagreements and link receipts instead of minting a story per grade.
